# Core pipeline
requests>=2.28
pandas>=1.5
numpy>=1.24

# Scoring models (IL-10 RF + IFNepitope2 compat shim)
scikit-learn>=1.3
joblib>=1.2

# Notebooks / visualization
matplotlib>=3.7

# IFN-gamma prediction (install with --no-deps to avoid pinned pandas==1.4)
# ifnepitope2>=1.2
